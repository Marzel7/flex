# EB1.0G Fixture Query Extractor

EB1.0G qualifies only a dependency-injected frozen/ephemeral SQLite source. It enforces `mode=ro`, verified `query_only`, exact tables/columns and document set, canonical JSON, one explicit EB0.1 revision, active deadlines, twelve rows and a 1 MiB ceiling before deterministic EB1.0C–E replay. It makes no production compatibility claim.
