================================================================================
   FUNDER WEBHOOKS + LISTENER TX CACHING: VISUAL IMPLEMENTATION GUIDE
================================================================================

TASK OVERVIEW
=============

┌─────────────────────────────────────────────────────────────────────────────┐
│ TASK B: LISTENER TX CACHING (67% savings)                                  │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  BEFORE: 3 RPC calls per migration                                         │
│  ┌────────────────────────────┐                                            │
│  │ Migration detected         │                                            │
│  └────────────┬───────────────┘                                            │
│               │                                                            │
│       ┌───────┴────────┬──────────────┐                                    │
│       │                │              │                                    │
│   RPC: get            RPC: get       RPC: get                             │
│   Transaction         Transaction    Transaction                          │
│   (mint)              (pool)         (blockTime)                          │
│   10 credits          10 credits     10 credits                           │
│                                                                            │
│  TOTAL: 30 CREDITS                                                         │
│                                                                            │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  AFTER: 1 RPC call + cached parsing                                        │
│  ┌────────────────────────────┐                                            │
│  │ Migration detected         │                                            │
│  └────────────┬───────────────┘                                            │
│               │                                                            │
│           RPC: get                                                         │
│           Transaction (CACHED)                                             │
│           10 credits                                                       │
│               │                                                            │
│       ┌───────┴────────┬──────────────┐                                    │
│       │                │              │                                    │
│      Parse           Parse           Parse                                │
│      (mint)          (pool)          (blockTime)                           │
│      0 credits       0 credits       0 credits                             │
│                                                                            │
│  TOTAL: 10 CREDITS (savings: 20 credits = 67%)                            │
│                                                                            │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│ TASK A: FUNDER WEBHOOK MONITORING (90-99% savings)                         │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  BEFORE: Polling all funders                                               │
│  ┌────────────────────────────────────────────┐                            │
│  │ Every 6 hours:                             │                            │
│  │                                            │                            │
│  │ Query creator_funders: 1,000-10,000 rows   │                            │
│  │ For each funder:                           │                            │
│  │   RPC: getSignaturesForAddress  (10 credits each)                       │
│  │                                            │                            │
│  │ TOTAL: 10,000-100,000 credits per cycle    │                            │
│  └────────────────────────────────────────────┘                            │
│                                                                            │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  AFTER: Webhook-driven monitoring                                          │
│  ┌────────────────────────────────────────────┐                            │
│  │ Once (setup):                              │                            │
│  │   Create Helius webhook                    │                            │
│  │   ~10 credits                              │                            │
│  │                                            │                            │
│  │ Then (ongoing):                            │                            │
│  │   Receive webhook events                   │                            │
│  │   ~0 credits (webhooks are free!)          │                            │
│  │                                            │                            │
│  │ TOTAL: ~10 credits (setup only!)           │                            │
│  └────────────────────────────────────────────┘                            │
│                                                                            │
│  MONTHLY SAVINGS: $200-500+ (depending on volume)                          │
│                                                                            │
└─────────────────────────────────────────────────────────────────────────────┘


IMPLEMENTATION FLOW
===================

TASK B: TX CACHING (Suggested first - simpler)
──────────────────────────────────────────────

  1. Update pumpfun_curve_listener.py
     ├─ Add imports (time, asyncio)
     ├─ Add cache dicts to __init__
     ├─ Add _get_transaction_cached() method
     ├─ Add _extract_mint_from_tx() method
     ├─ Add _extract_pool_from_tx() method
     ├─ Add get_tx_cache_stats() method
     ├─ Update handle_migration() to use cached TX
     └─ Update _ensure_db() with funder webhook schema

  2. Update main.py
     ├─ Add /api/listener/tx-cache-stats endpoint
     └─ Add funder webhook endpoints (5 routes)

  3. Create funder_watchlist_builder.py
     ├─ Watchlist scoring algorithm
     ├─ Risk tier assignment
     └─ Watchlist rebuild function

  4. Verify
     ├─ Check logs for [TX_CACHE] messages
     ├─ Test /api/listener/tx-cache-stats endpoint
     └─ Confirm cache hit rate improving over time


TASK A: FUNDER WEBHOOKS (Suggested second - more features)
───────────────────────────────────────────────────────────

  1. Database schema (in _ensure_db())
     ├─ funder_watchlist table
     ├─ funder_webhook_groups table
     ├─ funder_webhook_events table
     └─ Indexes for performance

  2. Watchlist builder
     ├─ Scoring algorithm (5 rules)
     ├─ Risk tiers (CRITICAL, HIGH, MEDIUM, LOW)
     └─ Run: python funder_watchlist_builder.py

  3. Add webhook receiver endpoint
     ├─ POST /api/webhook/funder
     ├─ Dedupe events by (signature, funder_address)
     └─ Store in funder_webhook_events

  4. Add UI endpoints
     ├─ GET /api/funder-watchlist/summary
     ├─ GET /api/funder-watchlist/top-risky
     └─ GET /api/funder-webhook-events

  5. Configure Helius
     ├─ Create webhook in Helius dashboard
     ├─ Point to /api/webhook/funder
     └─ Select event types (SOL_TRANSFER, TOKEN_TRANSFER)


TIMING & EFFORT
===============

Task B (TX Caching):
  Reading time:      15-30 minutes
  Implementation:    60-90 minutes
  Testing:           30 minutes
  Total:             2-3 hours
  Complexity:        LOW
  Risk:              VERY LOW

Task A (Webhooks):
  Reading time:      20-40 minutes
  Implementation:    90-120 minutes
  Testing:           45 minutes
  Total:             3-4 hours
  Complexity:        MEDIUM
  Risk:              LOW

Both together:
  Total effort:      5-7 hours
  Expected savings:  $200-500/month


KEY METRICS TO MONITOR
======================

Task B (TX Caching):
  ✓ tx_cache_hit_rate (target: >60% after warmup)
  ✓ getTransaction credits (target: -67% after implementation)
  ✓ Migration latency (target: 1500ms → 500ms)
  ✓ Cache size (should stabilize ~50-100 entries)

Task A (Funder Webhooks):
  ✓ Watchlist size by tier
  ✓ Webhook event rate (events/hour)
  ✓ Webhook delivery success rate
  ✓ RPC cost for funder monitoring (target: -90%)


VERIFICATION CHECKLIST
======================

After implementing Task B:
  [ ] Logs show [TX_CACHE] HIT messages
  [ ] /api/listener/tx-cache-stats returns valid JSON
  [ ] tx_cache_hit_rate increasing over time
  [ ] No errors in migration handling
  [ ] RPC credits for getTransaction reduced

After implementing Task A:
  [ ] Funder watchlist table populated
  [ ] Watchlist builder runs successfully
  [ ] /api/webhook/funder endpoint receives events
  [ ] Funder webhook events stored in database
  [ ] UI endpoints returning correct data


FILES TO MODIFY
===============

  pumpfun_curve_listener.py
    - Lines 307-320: Add cache init to __init__
    - Line ~320: Add _get_transaction_cached() method
    - Line ~900: Add _extract_mint_from_tx() method
    - Line ~1000: Add _extract_pool_from_tx() method
    - Line ~1970: Add get_tx_cache_stats() method
    - Line ~1789: Update handle_migration() with cache calls
    - Line ~572: Add funder webhook schema to _ensure_db()

  main.py
    - Add 6 new routes:
      ├─ POST /api/webhook/funder
      ├─ GET /api/listener/tx-cache-stats
      ├─ GET /api/funder-watchlist/summary
      ├─ GET /api/funder-watchlist/top-risky
      ├─ GET /api/funder-webhook-events
      └─ (5 endpoints total)

  funder_watchlist_builder.py (NEW FILE)
    - Create new standalone script
    - Run via: python funder_watchlist_builder.py
    - Or schedule as periodic task


RESOURCE USAGE
==============

Memory impact:
  Task B: ~100KB per 100 cached transactions (~2KB each)
  Task A: ~50KB for watchlist (50 entries) + events table

Database growth:
  Task A: ~1MB per 10,000 webhook events (~100 bytes each)

Network:
  Task B: Saves RPC calls (bandwidth reduction)
  Task A: Adds webhook payload traffic (minimal, high-signal)

CPU:
  Task B: Negligible (local parsing)
  Task A: Background job for scoring (runs once per 6h)


FAILURE MODES & RECOVERY
========================

Task B:
  ✗ Cache miss during peak traffic?
    → Falls back to direct RPC (temporary slowdown)
  ✗ Corrupted cache entry?
    → Expired after 30 min TTL, refetch
  ✗ Memory leak in cache?
    → TTL ensures cleanup, max ~100 entries

Task A:
  ✗ Webhook down?
    → Watchlist becomes stale but not critical
  ✗ Helius webhook fails?
    → Data loss for that period, can re-fetch manually
  ✗ Database corruption?
    → Rebuild watchlist: python funder_watchlist_builder.py


QUICK REFERENCE
===============

Start here:        UNIFIED_DIFF_GUIDE.md
Understand:        IMPLEMENTATION_SUMMARY.md
Technical detail:  FUNDER_WEBHOOK_IMPLEMENTATION.md
Copy-paste:        PATCH_*.py files

Questions:
  General:         See IMPLEMENTATION_SUMMARY.md "Questions & Support"
  Task B specific: See PATCH_LISTENER_TX_CACHE.py comments
  Task A specific: See PATCH_FUNDER_WEBHOOKS.py comments


EXPECTED OUTCOMES
=================

After 1 week:
  ✓ Both tasks implemented and tested
  ✓ Cache hit rate >50%
  ✓ Funder watchlist populated with ~50-500 entries
  ✓ RPC cost trending downward

After 1 month:
  ✓ Cache hit rate stabilized >60%
  ✓ Webhook events flowing regularly
  ✓ Estimated $150-400 monthly savings
  ✓ Real-time funder monitoring in place

After 3 months:
  ✓ Cache strategies optimized
  ✓ Watchlist scoring tuned to your data
  ✓ Webhooks running smoothly
  ✓ Potential further optimizations identified

================================================================================
Generated: 2026-03-05
For: Flex Token Analysis System
Optimization scope: RPC cost reduction + monitoring scale
================================================================================
