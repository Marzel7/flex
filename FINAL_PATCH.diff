===== PATCH 1: Bootstrap Synchronization =====
File: src/core/price_worker.py
Line: 349-365

BEFORE:
        bootstrap_thread = threading.Thread(target=bootstrap_task, daemon=True, name="PriceWorkerBootstrap")
        bootstrap_thread.start()
        
        # Start worker thread (will use whatever reserves are in PoolStateStore, which bootstrap is populating)
        logger.info("[PRICE_WORKER] Creating worker thread")

AFTER:
        bootstrap_thread = threading.Thread(target=bootstrap_task, daemon=True, name="PriceWorkerBootstrap")
        bootstrap_thread.start()

        logger.info("[PRICE_WORKER] Waiting for bootstrap to complete...")
        bootstrap_thread.join(timeout=2.0)
        if bootstrap_thread.is_alive():
            logger.warning("[PRICE_WORKER] ⚠️  Bootstrap still running after 2.0s (continuing anyway)")
        else:
            logger.info("[PRICE_WORKER] ✅ Bootstrap complete, starting worker loop")

        # Start worker thread (PoolStateStore now has reserves from bootstrap)
        logger.info("[PRICE_WORKER] Creating worker thread")

===== PATCH 2: Readiness Check Method =====
File: src/core/price_worker.py
Line: 1862 (before get_stats)

ADD:
    def has_pool_data(self, pool_address: str = None) -> bool:
        """Check if PoolStateStore has hydrated reserves for pool."""
        if pool_address:
            reserves = self._pool_state.get_pools_for_mint(pool_address)
            if reserves:
                for base_account, base_raw, quote_raw in reserves:
                    if base_raw and quote_raw and base_raw > 0 and quote_raw > 0:
                        return True
            return False
        else:
            all_mints = self._pool_state.get_all_mints()
            if not all_mints:
                return False
            for mint in all_mints:
                reserves = self._pool_state.get_pools_for_mint(mint)
                if reserves:
                    for base_account, base_raw, quote_raw in reserves:
                        if base_raw and quote_raw and base_raw > 0 and quote_raw > 0:
                            return True
            return False

===== PATCH 3: Per-Token Retry Loop =====
File: src/core/pumpfun_curve_listener.py
Line: 2022-2058

BEFORE:
    async def _extract_price_from_transaction(self, signature: str, token_mint: str) -> Optional[tuple]:
        """
        Extract on-chain price from pool vault balances.
        
        Strategy:
        1. Get pool address (from DB or extract from transaction)
        2. Try to query pool account balances (token and SOL)
        3. If fails, use DexScreener (more reliable)
        
        Returns: (price_usd, market_cap_usd, source) or None
        """
        try:
            pool_address = await self._get_pool_address(token_mint, signature)
            
            if pool_address:
                result = await self._get_price_from_pool_account(pool_address, token_mint)
                if result is not None:
                    price, market_cap = result
                    self.price_stats['onchain_success'] += 1
                    return (price, market_cap, "onchain")

            # Fall back to DexScreener (on-chain price extraction failed)
            self.price_stats['dexscreener_fallback'] += 1

AFTER:
    async def _extract_price_from_transaction(self, signature: str, token_mint: str) -> Optional[tuple]:
        """
        Extract on-chain price from pool vault balances.

        Strategy:
        1. Get pool address (from DB or extract from transaction)
        2. Retry on-chain price extraction with small delays (hydration guard)
        3. Fall back to DexScreener only after retries exhausted

        Returns: (price_usd, market_cap_usd, source) or None
        """
        try:
            pool_address = await self._get_pool_address(token_mint, signature)

            if pool_address:
                # HYDRATION GUARD: Retry on-chain extraction (pool data may not be ready immediately)
                for attempt in range(3):
                    result = await self._get_price_from_pool_account(pool_address, token_mint)
                    if result is not None:
                        price, market_cap = result
                        self.price_stats['onchain_success'] += 1
                        return (price, market_cap, "onchain")

                    if attempt < 2:
                        delay = 0.2 + (attempt * 0.1)
                        await asyncio.sleep(delay)

            # Fall back to DexScreener only after all retries exhausted
            self.price_stats['dexscreener_fallback'] += 1

===== PATCH 4: Pool Extraction Hydration Guard =====
File: src/core/pumpfun_curve_listener.py
Line: 2200

BEFORE:
            if token_balance == 0 or sol_balance == 0:
                return None

AFTER:
            # HYDRATION GUARD: Require both base AND quote to be ready
            if token_balance <= 0 or sol_balance <= 0:
                return None

===== PATCH 5: Signal-Based Readiness Wait =====
File: src/core/pumpfun_curve_listener.py
Line: 2888-2900

BEFORE:
            # Persist telemetry (retry_count=0 for primary fast-lane path)
            await self._write_resolution_telemetry(mint, discovery_source, pool_address, 0)
            
            # Trigger WebSocket refresh
            if self.price_worker:

AFTER:
            # Persist telemetry (retry_count=0 for primary fast-lane path)
            await self._write_resolution_telemetry(mint, discovery_source, pool_address, 0)

            # HYDRATION GUARD: Wait for pool data to be ready before triggering price extraction
            pool_ready = False
            for attempt in range(5):
                if self.price_worker and self.price_worker.has_pool_data(pool_address):
                    pool_ready = True
                    break
                await asyncio.sleep(0.2)

            if not pool_ready:
                log_print(f"[FAST_PATH_REGISTER] ⚠️  Pool {pool_address[:16]}... reserves not ready after 1.0s (continuing anyway)", flush=True)

            # Trigger WebSocket refresh
            if self.price_worker:

===== PATCH 6: Price Worker Hydration Guard =====
File: src/core/price_worker.py
Line: 1161-1165

BEFORE:
                for base_account, base_raw, quote_raw in pool_reserves:
                    # ✅ CRITICAL: Guard against invalid reserves (shouldn't reach here but double-check)
                    if base_raw <= 0 or quote_raw <= 0:
                        logger.debug(f"[PRICE_DEBUG] {mint[:16]}... ✗ skipping invalid reserves: base={base_raw}, quote={quote_raw}")
                        continue

AFTER:
                for base_account, base_raw, quote_raw in pool_reserves:
                    # ✅ HYDRATION GUARD: Require both base AND quote reserves to be fully hydrated
                    if not base_raw or not quote_raw or base_raw <= 0 or quote_raw <= 0:
                        logger.debug(f"[PRICE_DEBUG] {mint[:16]}... ✗ skipping unhydrated reserves: base={base_raw}, quote={quote_raw}")
                        continue

===== SUMMARY =====
Files modified: 2
Total changes: 6 patches
Lines added: ~50
Risk level: Very Low
Status: ✅ All syntax verified, ready for production
